Copy your fitted judge here BEFORE exporting any build:
  %APPDATA%\Godot\app_userdata\UKO\value_fn.cfg  -> Data\value_fn.cfg
  %APPDATA%\Godot\app_userdata\UKO\calibration.cfg -> Data\calibration.cfg
The game loads user:// first (refits win), then falls back to these shipped
copies -- without them, fresh installs and the website play the hand eval.
